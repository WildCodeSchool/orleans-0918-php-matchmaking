<nav class="navbar navbarProjetIO navbar-expand-lg navbar-dark">
    <div class="col-3">
        <a class="navbar-brand" href="https://www.projet.io/">Projet <span>I/O</span></a>
    </div>
    <div class="col-9 d-flex justify-content-end pr-0" id="navbarNavProjetIO">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a href="{{ path('app_logout') }}" role="button" class="btn border border-primary">
                    <i class="fas fa-sign-in-alt"></i>
                    {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_MANAGER') %}
                        Deconnexion
                    {% else %}
                        Connexion
                    {% endif %}
                </a>
            </li>
        </ul>
    </div>
</nav>
{% if is_granted('ROLE_MANAGER') %}
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="{{ path('event_index') }}">MatchMaking</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('event_index') }}">Gestion des événements</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('manager_index') }}">Gestion des gestionnaires</a>
                </li>
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_index') }}">Gestion des admins</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('society_index') }}">Gestion des sociétés</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('settings') }}">Paramètres</a>
                    </li>
                {% endif %}

            </ul>
        </div>
    </nav>
{% endif %}