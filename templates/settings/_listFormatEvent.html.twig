<div class="row mt-4">
    <div class="col">
        <h3>Liste des formats d'événements</h3>
        <p class="mt-3">Les formats suivants sont présents dans la base de données.</p>
    </div>
</div>

<div class="row mt-3">
    {% if formatEvents is defined and formatEvents|length > 0 %}
        {% for formatEvent in formatEvents %}
            <div class="col-lg-3">
                <p class="mx-auto text-center py-2 border border-secondary">
                    {{ formatEvent.name }}
                </p>
            </div>
        {% endfor %}
    {% else %}
        <p>Aucun format d'enregistré.</p>
    {% endif %}
 </div>