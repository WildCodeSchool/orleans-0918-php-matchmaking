<h1>Gestion des paramètres</h1>
<div class="row">
    <div class="col">
        <h2 class="mt-5">Gestion des Timers</h2>
    </div>
</div>
{{ form_start(formTimer) }}
<div class="row mt-2">
    <div class="col-md-3 col-ms-6">{{ form_row(formTimer.roundMinutes, {'label': 'Temps de tour (minutes)'}) }}</div>
    <div class="col-md-3 col-ms-6">{{ form_row(formTimer.roundSeconds, {'label': 'Temps de tour (secondes)'}) }}</div>
    <div class="col-md-3 col-ms-6">{{ form_row(formTimer.pauseMinutes, {'label': 'Temps de pause (minutes)'}) }}</div>
    <div class="col-md-3 col-ms-6">{{ form_row(formTimer.pauseSeconds, {'label': 'Temps de pause (secondes)'}) }}</div>
</div>
<button class="btn btn btn-primary btn-lg btn-block">{{ button_label|default('Valider') }}</button>
{{ form_end(formTimer) }}
